Add `script/bench_cut_one.py` — B4 CUT_ONE diagnostic for IIT 4.0 (`SET_UNI/BI`). Filters the full partition list to singleton-isolation 2-part `GeneralSetPartition` objects (3N candidates after `unique()` deduplication vs Bell(N) exhaustive). Evaluates with exact GID and compares against exhaustive `sia()`. Key finding: yields 12× (N=4) and 71× (N=5) speedup; `co_normalized_phi ≥ full_normalized_phi` always holds (correct upper bound), but PhiMatch=✗ for both networks since both true MIPs are 4-part partitions unreachable by 2-part CUT_ONE candidates.
