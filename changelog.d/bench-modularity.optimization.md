Add `script/bench_modularity.py` — B8 max-modularity partition diagnostic. Builds pairwise-MI-weighted graph, runs `networkx.community.greedy_modularity_communities` (deterministic, O(E log N)), maps communities to matching `GeneralSetPartition` direction-variants, evaluates with exact GID vs exhaustive `sia()`. Key finding: produces identical communities to Louvain (B3) on both benchmark networks; same failure modes (micro's MIP is 4-part, ring CA's uniform MI yields 1 community). Marks B8 as Done; all actionable baselines B1–B4, B7, B8 are now implemented.
